#!/usr/bin/env bash
# Python environment installation script

# Brew depends and the main python
brew install python python3 freetype qt pyqt zmq swig

# Creating virtualenv environment
brew install virtualenv virtualenvwrapper
mkdir $HOME/.virtualenv

# Installing Numpy
brew install numpy

# Installing SciPy
brew install scipy

# Installing matplotlib
brew install matplotlib

# Installing IPython
pip install ipython pyzmq pygments
